id: add roles
author: Default
realm: local_realm
changes:
  - addRole:
      name: user
      description: Example role with user access to application data
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: betrieb
      description: Example role with betrieb access to application data
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: admin
      description: Example role with admin access to application data
      clientRole: true
      clientId: local
      composite: false
